# 2026-08-19 至 2026-08-20 开发记录（v76）

## 概述

本阶段在上一份记录（2026-08-02 至 2026-08-18）基础上继续收尾：移除 Agent 客户端与开发测试脚手架（保留对外部 Agent 的 API 服务端），并完成一批开发者选项、随口记、WebDAV 调试和模型配置的交互与功能调整，同时统一了随口记与日程列表条目的圆角风格。

当前构建版本：

- `versionName = 2.3.0`
- `versionCode = 113`（由 112 提升）
- `minSdk = 33`
- `targetSdk = 36`

## 1. Agent 客户端与开发测试移除（保留外部 Agent API）

- 删除 `shared/api/client/WillDoAgentClient.kt`（Agent 客户端，仅内部开发/测试用），保留外部 Agent 的 API 服务端（`WillDoAgentProvider`、`AgentDataApi`、`AgentDataService`、`AgentDatabaseService`、`AgentProtocolJson` 等）。
- 删除对应测试：`AgentProtocolJsonTest.kt`、`AgentEventQueryResolverTest.kt`、`ConfigCatalogAgentAccessTest.kt`。
- 删除 `eta/` 空包占位目录（data/integration/model/runtime/ui）。
- 回退「Agent → Eta」文案改动（`AiSettingsPage`、`ConfigCatalog` 恢复为「Agent」表述），Agent 能力本身不变。

## 2. 日程入库去重（原「短信取件码去重」）

- 配置项由「短信取件码去重」改为「日程入库去重」：字段 `smsPickupDedupEnabled` 重命名为 `scheduleIngestDedupEnabled`，配置 key、label、description 同步更新。
- `ScheduleIngestWriter` 去重逻辑统一：移除 `sourceType != "sms"` 特例，短信取件码与识别结果入库统一受该开关控制；关闭开关后识别结果入库也不再去重。
- 涉及 `MySettings`、`ConfigCatalog`、`SettingsViewModel`、`DeveloperPage`、`DeveloperUiContract`、`SmsPickupIngestCoordinator`。

## 3. 开发者页恢复「移除捐赠标记」按钮

- 在开发者页「通知与日志」卡片底部恢复「移除捐赠标记」入口（对应早期版本 `onRemoveDonationThanks` 行为），点击调用 `updateHasDonated(false)` 清除「感谢您的捐赠」标记。

## 4. 文本待办完成框移除

- 随口记列表项右侧的待办完成框 `QuickMemoTodoMark` 改为仅非 TEXT 类型待办显示，文本待办不再显示完成框。

## 5. WebDAV 调试迁移到「配置编辑 - 同步」

- 从开发者首页移除「WebDAV 调试」板块。
- 在 `ConfigEditorPage` 的 SYNC 域新增「WebDAV 测试根目录覆盖」卡片（含路径校验与 `resetRemoteTracking`），新增 `UpdateWebDavRemoteRoot` 动作路由；`SettingsViewModel` 中旧的 `updateWebDavRemotePathOverride` 一并清理。

## 6. 空白随口记自动删除

- `QuickMemoDetailPage` 增加 `DisposableEffect`，离开详情时若该条目仍是空白纯文本随口记（无正文、无图片、无语音），自动删除。

## 7. 模型配置支持自定义模型名称

- `AiConfigForm` 在模型列表为空时，「模型名称」下拉改为文本输入，可直接填写软件预设/拉取列表之外的模型名。
- 自定义 provider 已手填模型名时可直接保存，不再强制先拉取模型列表。

## 8. 随口记条目圆角统一为直角

- 移除 `QuickMemoListItem` 卡片上新增的 `clip(RoundedCornerShape(16.dp))`，条目改回直角，与日程条目（`SwipeableEventItem` 无 shape）风格一致。
- 同时修复圆角裁切左侧颜色标签与边缘文字的问题。

## 验证

已执行：

```text
:app:compileNativeDebugKotlin
checkArchitectureGuardrails
:app:assembleNativeDebug
```

结果为 `BUILD SUCCESSFUL`，架构守卫通过。构建保留既有警告（未检查类型转换、已弃用 API 等），不影响本次改动。
