# Architecture guardrail baseline
# Format: RULE_ID|relative/path/to/file.kt
# Keep this file as small as possible. Remove entries once migrated.
# MAIN_UI_EDITION_USAGE may only be baselined for build/display metadata, never business branching.
