# 2026-03-17 开发记录 (v22版本)

## 需求背景

用户目标：
1) 仅按小米“模板 10”接入 HyperOS 超级岛通知，展示动态标题/备注/按钮。
2) 保持原生胶囊与 Flyme 胶囊逻辑不受影响。
3) 当前问题：通知已发出但系统未识别为超级岛，需要排查并修复。

---

## 一、本次实现范围

### 1) HyperOS 路由与小米 Provider

**修改文件**：
- `app/src/main/java/com/antgskds/calendarassistant/service/capsule/CapsuleService.kt`
- `app/src/main/java/com/antgskds/calendarassistant/service/capsule/provider/XiaomiCapsuleProvider.kt`

**核心改动**：
1. HyperOS 环境切换到 `XiaomiCapsuleProvider`。
2. 在 XiaomiProvider 中构建模板 10 需要的 `baseInfo/picInfo/hintInfo` 与 pics Bundle。

### 2) Xiaomi 模型与必填字段补齐

**修改文件**：
- `app/src/main/java/com/antgskds/calendarassistant/data/model/xiaomi/XiaomiCapsuleModels.kt`

**核心改动**：
1. `baseInfo/picInfo/hintInfo` 的 `type` 使用 `@EncodeDefault(ALWAYS)` 强制输出。
2. 新增 `param_v2` 相关模型（含 `param_island`、岛摘要结构）。

### 3) 兼容 HyperOS 解析的 extras 结构

**修改文件**：
- `app/src/main/java/com/antgskds/calendarassistant/service/capsule/provider/XiaomiCapsuleProvider.kt`

**核心改动**：
1. 继续写入 `miui.focus.baseInfo/picInfo/hintInfo/pics`。
2. 追加 `miui.focus.extra`、`miui.focus.focusExtra`、`miui.focus.param`，内容为 `param_v2`。
3. `param_v2` 包含 `protocol/business/ticker/enableFloat/param_island`，岛摘要区域使用应用图标 key。

### 4) Action Intent 广播能力

**修改文件**：
- `app/src/main/AndroidManifest.xml`

**核心改动**：
1. `EventActionReceiver` 设为 `exported=true`，满足 actionIntentType=2 的系统要求。

---

## 二、排查记录

1. dumpsys 仍出现 `focusType=null`，系统日志未见 FocusPlugin 解析记录。
2. 发现只触发 **popup 通知**（`calendar_assistant_popup_channel_v2` + `AUTO_CANCEL`），
   **未出现 ongoing/foreground 通知**，因此当前无法验证超级岛识别链路。
3. 已补齐 `param_v2` 结构，待在 live 通知上验证系统解析结果。

---

## 三、待完成事项

- [ ] 触发“进行中”事件，确认 live 通知出现 `ONGOING_EVENT/FOREGROUND_SERVICE`。
- [ ] 在 live 通知中核对 `miui.focus.param`/`param_v2` 是否被系统解析。
- [ ] 若仍 `focusType=null`，进一步排查系统开关或白名单限制。
